# PostgreSQL connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/eers

# Long random secret used to sign application sessions (minimum 32 characters)
EERS_SESSION_SECRET=replace-with-a-long-random-secret-at-least-32-characters

# Optional: AI provider configuration (never commit real keys)
AI_PROVIDER=
AI_API_KEY=
