# PostgreSQL connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/eers

# Long random secret used to sign application sessions
SESSION_SECRET=replace-with-a-long-random-secret

# Optional: AI provider configuration (never commit real keys)
AI_PROVIDER=
AI_API_KEY=
